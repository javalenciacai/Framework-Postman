# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml




pool:
  vmImage: 'ubuntu-latest'

steps:
- task: CmdLine@2
  displayName: 'Install Newman'
  inputs:
    script: 'sudo npm install -g newman'
- task: CmdLine@2
  displayName: 'Install Newman-Reporter-htmlextra'
  inputs:
    script: 'sudo npm install -g newman-reporter-htmlextra'   
- task: CmdLine@2
  displayName: 'PayrollLiquidator Test Execution'
  continueOnError: true
  inputs:
    script: 'newman run variableurlglobal.postman_collection.json -e https://dev.azure.com/Prueba1james/Prueba1/_apis/sourceProviders/TfsGit/filecontents?repository=testVariableGlobal&path=%2FurlQA.json&commitOrBranch=master&api-version=5.0-preview.1 --reporters cli,junit,htmlextra --reporter-junit-export results/junitReport.xml --reporter-htmlextra-export ./results/index.html'
    workingDirectory: '$(System.DefaultWorkingDirectory)/Postman'

- task: PublishTestResults@2
  inputs:
    testResultsFormat: 'JUnit'
    testResultsFiles: 'Postman/results/*.xml'
    testRunTitle: 'PayrollLiquidator'

- task: UploadPostmanHtmlReport@1
  inputs:
    cwd: '$(System.DefaultWorkingDirectory)/Postman/results'
    tabName: 'PayrollLiquidator'